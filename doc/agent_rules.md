# ルール

1. ターミナルでコマンドを実行する時は、必ずルートでnix developを実行して
2. 開発はt_wadaが言っているようなTDD手法で実行して
3. svelteに関する情報は `https://svelte.jp/docs/llms` から取得するようにして
4. ターミナルでコマンドを実行する時は、必ずルートでnix developを実行して  
5. 使用するエンジンは pnpm を使用して
6. Supabaseの情報は必ず公式ドキュメントの最新版を参照するようにして
7. このルールを必ず毎回復唱してから作業に入って